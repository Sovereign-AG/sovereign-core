"use client";

import React from 'react';

interface SVTPLogoProps {
  size?: number;
  className?: string;
}

/**
 * LOGO DEACTIVATED: Per USER REQUEST, the graphical brand mark is removed.
 * We will revisit branding at a later stage.
 */
export const SVTPLogo = ({ size = 24, className = "" }: SVTPLogoProps) => {
  return null;
};

