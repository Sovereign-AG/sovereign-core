"use client";

import React from 'react';

interface SovereignLogoProps {
  size?: number;
  className?: string;
}

/**
 * LOGO DEACTIVATED: Per USER REQUEST, the graphical brand mark is removed.
 * We will revisit branding at a later stage.
 */
export const SovereignLogo = ({ size = 24, className = "" }: SovereignLogoProps) => {
  return null;
};
